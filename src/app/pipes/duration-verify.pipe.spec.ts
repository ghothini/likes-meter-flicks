import { DurationVerifyPipe } from './duration-verify.pipe';

describe('DurationVerifyPipe', () => {
  it('create an instance', () => {
    const pipe = new DurationVerifyPipe();
    expect(pipe).toBeTruthy();
  });
});
